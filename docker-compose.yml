services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8200:8200"
    volumes:
        - ./docker/data:/root/.local/share/gomarks
        - ./docker/cache:/root/.cache/gomarks
    restart: unless-stopped
